# Any

PP2 types share a common base type, called `Any`. `Any` proves default implementations of several basic operations, like equality comparison, type resolution, casting, and stringization.

```{livecode}
PP2 (
    print((I am a quote)),
    print((I am a quote) type)
)
```
